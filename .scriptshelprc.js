module.exports = {
  "help-message": [
    " This project is meant to be run locally only",
    "                                             ",
  ],
  "refresh": "Refresh (in watch mode) `server/db.json` using the seed configured in `.env` file",
  "serve": "Start json-server (in watch mode) using `server` directory",
  "start": "Run `refresh` and `serve`"
}
